// هذا الملف يستخدم لتوفير بيانات ديناميكية أو وظائف مساعدة عامة لـ Eleventy.
// يمكن الوصول إلى القيم والوظائف المعرفة هنا في أي قالب Nunjucks.

module.exports = {
  // خاصية ديناميكية للحصول على السنة الحالية
  // يمكن استخدامها في قوالب Nunjucks كـ `{{ global.currentYear }}` لحقوق النشر مثلاً.
  get currentYear() {
    return new Date().getFullYear();
  }

  // مثال لوظيفة مساعدة (يمكن إضافتها هنا إذا كانت متطلبات معالجة البيانات معقدة)
  /*
  formatPhoneNumber: (number) => {
    // وظيفة لتنسيق أرقام الهواتف
    return number.replace(/(\d{3})(\d{3})(\d{4})/, '$1-$2-$3');
  }
  */
};

